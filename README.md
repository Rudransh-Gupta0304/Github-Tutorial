for git and github 
to create any other branch we use                                      // git branch <name>
to switch between the master and the create one we use                 // git switch <name>
to inspect the current branch we use                                   // git branch 

next command is                                                        // git switch -c dark mode 
what this command do is banane ke saath saath us branch 
me switch bhi kr lega

and the next one is checkout command what this do is ki 
check karega if that is existing or not agar , nhi krega 
than mana kar dega if yes than fir vo uspe switch kr dega              // git checkout <name>

git diff is used to tell the diff between the commit id 
or is used specifically to draw the comparison between to changes.     // git diff id1..id2


stash is way to save you changes in a temporary location               // git stash